version: "3.9"
services:
  dev:
    build: .
    ports:
      - '8888:8888'
      - '8889:8889'
    volumes:
      - ./dist:/dist
      - .:/work
    # docker-compose exec dev python -m build -o /dist

